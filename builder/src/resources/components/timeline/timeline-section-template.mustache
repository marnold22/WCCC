<!DOCTYPE html>

<div class="demo-card demo-card--step{{number}} ">
    <div class="head">
        <div class="number-box">
            <span>{{number}}</span>
        </div>
        <h2><span class="small">{{subtitle}}</span> {{title}}</h2>
    </div>
    <div class="body">
        <p>{{description}}</p>
        <img src="{{imageRef}}" alt="Graphic"/>
    </div>
</div>
